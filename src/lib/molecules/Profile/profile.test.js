import React from 'react';

import { checkSnapshot } from 'testUtils';
import Profile from '.';

describe('Molecules: Profile', () => {
    checkSnapshot('Profile snapshot', <Profile />);
    // test cases
});
