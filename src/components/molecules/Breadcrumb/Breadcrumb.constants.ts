export const BREADCRUMB_ICON_SIZE = 20;
export const BREADCRUMB_SEPARATOR_SIZE = 24;

export const MAX_VISIBLE_BREADCRUMB_ITEMS = 6;
export const MAX_TRUNCATED_VISIBLE_ITEMS = 4;
/** Max items shown on either side (first or last) when truncated. */
export const MAX_VISIBLE_ITEMS_PER_SIDE = 2;
