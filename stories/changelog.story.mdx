import { Meta, Description } from '@storybook/addon-docs';
import ChangelogMD from '../CHANGELOG.md';

<Meta
    title="Changelog"
    parameters={{
        previewTabs: {
            canvas: {
                hidden: true
            }
        },
        viewMode: 'docs'
    }}
/>

<div style={{display: "flex", "justify-content": "center"}}>
    <Description markdown={ChangelogMD} />;
</div>
